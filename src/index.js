import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import reportWebVitals from './reportWebVitals';
import TableExampleSortable from "./App";


ReactDOM.render(
  <React.StrictMode>
    <TableExampleSortable/>
  </React.StrictMode>,
  document.getElementById('root')
);

reportWebVitals();
